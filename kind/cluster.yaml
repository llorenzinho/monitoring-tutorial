apiVersion: kind.x-k8s.io/v1alpha4
kind: Cluster
name: monitoring-tutorial

nodes:
  - role: control-plane
    extraPortMappings:
      - containerPort: 30800
        hostPort: 8800
        protocol: TCP
  - role: worker
  - role: worker
